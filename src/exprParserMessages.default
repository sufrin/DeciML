
(* This file could have been, but wasn't, auto-generated based on "exprParserMessages". *)

(* Please note that the function [message] can raise [Not_found]. *)

let message = fun s-> raise Not_found

